{
  "name": "etsy-draft-listing-app",
  "version": "0.1.0",
  "private": true,
  "description": "Local desktop-style web app to generate Etsy draft listings from synced photos + intake form using OpenAI",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
